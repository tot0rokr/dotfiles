# ~/.tmux.conf - entry point. Shared config lives in ~/.tmux.common.conf.
# Add this host's own tmux settings below (e.g. tool integrations).
source-file ~/.tmux.common.conf

# ============================================================
# Machine-specific settings below (edit per host)
# ============================================================
